<!-- webapp/fragments/MyFragment.fragment.xml -->
<core:FragmentDefinition id="frg1"
    xmlns="sap.m"
    xmlns:core="sap.ui.core" >
    <VBox id="vbox1Container" class="sapUiSmallMargin">
        <Label id="label1Comments" text="Mission du titulaire" />
        <TextArea
            id="text1AreaComments"
            width="100%"
            rows="5"
            placeholder="Type your text here..."
            liveChange=".onLiveChange" />
    </VBox>

    <VBox id="vbox2Container" class="sapUiSmallMargin">
        <Label id="label2Comments" text="Données de base" />
        <TextArea
            id="textArea2Comments"
            width="100%"
            rows="5"
            placeholder="Type your text here..."
            liveChange=".onLiveChange" />
    </VBox>

    <VBox id="vbox3Container" class="sapUiSmallMargin">
        <Label id="label3Comments" text="Période et délais" />
        <TextArea
            id="textArea3Comments"
            width="100%"
            rows="5"
            placeholder="Type your text here..."
            liveChange=".onLiveChange" />
    </VBox>

    <VBox id="vbox4Container" class="sapUiSmallMargin">
        <Label id="label4Comments" text="Mission sous-traitées" />
        <TextArea
            id="textArea4Comments"
            width="100%"
            rows="5"
            placeholder="Type your text here..."
            liveChange=".onLiveChange" />
    </VBox>

    <VBox id="vbox5Container" class="sapUiSmallMargin">
        <Label id="label5Comments" text="Gestion des interfaces" />
        <TextArea
            id="textArea5Comments"
            width="100%"
            rows="5"
            placeholder="Type your text here..."
            liveChange=".onLiveChange" />
    </VBox>


</core:FragmentDefinition>