<core:FragmentDefinition xmlns:core="sap.ui.core" xmlns="sap.m">
    <Table id="workflowTable"
           inset="false"
           mode="None"
           items="{wf>/results}">
        
        <!-- Columns -->
        <columns>
            <Column>
                <Text text="Étape du workflow" />
            </Column>
            <Column>
                <Text text="Statut" />
            </Column>
            <Column>
                <Text text="Destinataire principal" />
            </Column>
        </columns>

        <!-- Rows (template) -->
        <items>
            <ColumnListItem>
                <cells>
                    <Text text="{wf>ETAPE}" />
                    <Text text="{wf>STATUS}" />
                    <Text text="{wf>AGENT}" />
                </cells>
            </ColumnListItem>
        </items>
    </Table>
</core:FragmentDefinition>
